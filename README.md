## Running

- Start a local server:

  ```
  git clone https://github.com/relayjs/relay-examples
  cd relay-examples/todo
  npm install
  npm start
  ```

- Start the demo

  ```
  cd react-relay-demo
  npm install
  npm run relay
  npm start
  ```
